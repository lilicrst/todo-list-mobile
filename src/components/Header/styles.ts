import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  header: {
    width: '100%',
    height: 173,
    backgroundColor: '#0D0D0D',
    alignItems: 'center',
    marginBottom: 0 - 30
  },
  todoLogo: {
    width: 110,
    height: 32,
    marginTop: 70,
    flexDirection: "row"
  },
  logoText: {
    fontSize: 30,
    fontWeight: "bold",
    color: "#5E60CE"
  }
})